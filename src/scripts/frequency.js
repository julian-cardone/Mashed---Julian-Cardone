class Frequency{

    constructor(context, bpmBox){
        this.context = context;
        this.bpmBox = bpmBox;
        this.fillBox(this.context, this.bpmBox);
    }

    fillBox(context, bpmBox){
        
    }

}